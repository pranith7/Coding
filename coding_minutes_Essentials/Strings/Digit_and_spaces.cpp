#include<iostream>
#include<cstream>

int main()
{

    char ch;
    ch = cin.get();

    int alpha = 0;
    int numeric = 0;
    int spaces = 0;

    while(ch!="\n")
    {
        if(ch>='0' and ch<='9') numeric++;
        else if (ch/>=)
        ch = cin.get();
    }

    return 0;
}